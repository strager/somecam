# Get an API key from https://console.x.ai/
XAI_API_KEY=your-key-here

# Optional PostHog project key for analytics proxying
# POSTHOG_KEY=phc_your_posthog_key_here

# Set to log AI requests and responses to the console
# DEBUG_PROMPT=1

# DocRaptor API key for server-side PDF generation
# DOCRAPTOR_API_KEY=your-key-here

# Set to use live DocRaptor mode (default is test mode with watermarked PDFs)
# DOCRAPTOR_LIVE=1

# HMAC secret for proof-of-work challenge signing (random per startup if unset)
# POW_SECRET=your-secret-here

# Path to the rate-limit SQLite database (default: ./data/rate-limit.sqlite)
# RATE_LIMIT_DB_PATH=./data/rate-limit.sqlite

# Enable cleanup of expired sessions and challenges (disabled by default)
# RATE_LIMIT_CLEANUP=1
